# English language and typography audit — 2026-09-20

The whole-site audit is still in progress. Passing the checks below does not mean every administrator workflow or live authenticated role has been verified.

## Implemented

- Replaced partial word substitutions with complete-copy translations so unknown sentences do not become mixed-language fragments.
- Public published copy, course registration, notification workflows, coach profiles, and policy pages have English translations.
- English navigation, hero typography, and mobile coach names fit their available space. Mobile profile phone fields give English country labels enough room.
- Student dashboard, profile editing, badges, attendance, and makeup-session controls have English copy. Dates and countdowns follow the selected language.
- Student feedback option values remain unchanged when their displayed labels are translated.
- Coach workspace, registration details, attendance, teaching check-in, leave forms, and calendar have English copy and localized dates. Native cancellation confirmations follow the selected language.
- Finance access, transaction matching, file mapping, confirmations, and read-only states have English copy. The upload button uses website text instead of browser-native text.
- Option-group labels participate in language switching without changing the selected option values. Unsaved-change confirmations follow the selected language.
- Administrator navigation, season overview, student/coach account management, and payment-account forms now have English interface copy and locale-aware dates.
- Enrollment review includes English statistics, sync notices, registration/order details, billing exceptions, and native payment/delete confirmations. CSV headers and filenames follow the selected language; authored notes and identifying values remain intact.
- Administrator coach-duty details, leave review, substitute assignment, emergency coverage, and manual attendance correction have English labels, dates, and validation messages. Authored leave reasons and audit notes remain unchanged.
- Shop search matches displayed English names; product details, cart, checkout, payment status and error messages follow the selected language. Stored specification values, cart identifiers and customer notes remain unchanged.
- Product administration includes English fields, validation, image cropping, unsaved-change confirmations and delete dialogs.
- Content and season editors have English field labels, help text, media controls, validation, statuses and native confirmations. Coach/photo and badge labels translate their interface wording without rewriting form values.
- Content navigation descriptions wrap instead of clipping. Milestone image fields have enough space for previews and controls. A failed slideshow image upload keeps the crop dialog open with an error and allows retry.
- Changing language in the coach planner no longer refetches and overwrites an unsaved workout. Week labels update independently; stale requests from a previous student or week are ignored. Mobile workout fields use 16px text and date-specific accessible labels. History and CSV exports retain coach-authored text.
- Planner API validation messages, 404 pages, error details and website-content loading/failure/retry prompts have English copy. Raw diagnostic details retain their original text.
- Coach attendance and substitution API responses have English copy, including expired check-in windows, stale invitations, administrator confirmation and cancelled sessions. Action feedback is shown and scrolled into view inside the open session dialog. Leave guidance uses a separate line so longer English titles retain their space.
- Enrollment, supplement, account and student-action responses have English copy for expired quotes, full classes, archived payment instructions, stale submissions, email delivery, attendance and makeup validation. Failed submissions retain their input values.
- Stored training feedback now renders generated labels and selected sleep/fatigue/completion values in English on both student and coach pages. Authored feelings, pain descriptions, notes and filenames remain verbatim; incomplete or plain legacy notes are not treated as structured records. The student screenshot picker uses website labels instead of browser-native file-control text.
- Administrator and legacy-enrollment API response text has complete English entries. Generated quarter names, class-copy suffixes, existing-season warnings, new-class results, incomplete billing counts and product deletion results translate as complete messages. Duplicating a season now refers to the source season instead of incorrectly naming Q3.
- The anniversary interest form uses a complete localized required-name message instead of concatenating Chinese with an English field label. Mobile inputs, selection and textareas use 16px text; original submitted names, contact details, notes and option values remain intact.
- Public payment-setting errors and publication results now have complete English copy, including concurrent edits, invalid bank/QR details, access denial, failed reads and successful persistence followed by a failed readback. The QR image alternative text is translated. Mobile payment-account creation errors appear inside the open form, receive focus and scroll into view while retaining entered values.
- Login/register throttling, service failures and field-validation responses now have English copy. Website media upload permissions, format/size limits and integration-script errors are translated, including both 8 MB website-image and 15 MB coach-image limits.

## Evidence

The role scripts run against a local preview with synthetic accounts. Every business API and external network request is intercepted; no real student, coach, bank, or email record is changed.

| Check | Verified scope |
| --- | --- |
| `audit-english-student.mjs` | 19 states: enrollment gates, populated/empty/error dashboard, settings, feedback submission, profile/badges, profile save, attendance, leave and makeup scheduling; 1440px and 375px |
| `LANGUAGE_STUDENT_ACTIONS=1 audit-english-student.mjs` | 32 strict desktop/mobile states: structured feedback readback, chosen filename, leave conflict/success, full/cancelled makeup sessions, cancellation errors/success, feedback ownership/RPE errors, invoice/profile errors and training/attendance access errors. Explicit assertions inspect translated structured fields even inside the authored-text opt-out container. |
| `LANGUAGE_ENROLLMENT_ACTIONS=1 audit-english-interactions.mjs` | 16 desktop/mobile states: expired quote, full class, archived payment instructions, stale/invalid supplements, stale staff review, unconfirmed email delivery and email retry. Form values, Chinese notes and leading zeroes remain unchanged; no real registrations or emails are sent. |
| `audit-english-coach.mjs` | 28 states: workspace, check-in and leave forms, populated and empty rosters, expanded registration details, attendance save, cancellation/restoration, mobile calendar, and planner initial states |
| `LANGUAGE_COACH_DUTY=1 audit-english-coach.mjs` | 42 strict desktop/mobile states: expired check-in window, leave conflict, invited/admin-arranged leave, stale invitation, accepted/declined substitutions with and without administrator confirmation, manual entry, re-invitation, refresh failure, upcoming/missing-time/late/absent/cancelled/approved-leave states, same-day class switching and access denial. Action feedback must be inside the open dialog; submitted reasons and invitation IDs remain intact. |
| `audit-english-finance.mjs` | 18 states: locked/unlocked access, incorrect password, matching results, batch confirmation, CSV column preview, password setup, temporary lock, archived season, read-only access, empty batches, and unauthorized account |
| `verify-language-dom.mjs` | Mutation batches, translated attributes and option-group labels, unchanged option/input values, opt-out text, document titles, and English font stack |
| `audit-english-admin.mjs` | 53 checks: desktop/mobile workspaces, coach-duty details and action dialogs, required reasons, attendance correction, registration and shop-order details, payment validation/confirmation, billing resolution, synchronization dialog, CSV headers, mobile student tabs/coach assignment and payment-account validation |
| `audit-english-shop.mjs` | 40 states at 1440px and 375px, including five product details, English search, cart, checkout error/success, payment states, stale specifications, empty and failed loads; strict mode passed |
| `LANGUAGE_ADMIN_PRODUCTS=1 audit-english-admin.mjs` | 28 desktop/mobile states: product editing, specification validation, image crop/upload, save/discard, create/delete and custom sizes; no untranslated interface text or horizontal overflow |
| `LANGUAGE_ADMIN_CONTENT=1 audit-english-admin.mjs` | 102 strict desktop/mobile states: every content section, draft/restore/save, coach profile, hero/avatar cropping, image failure/retry and video upload failure, season settings, native delete/activate/discard confirmations, course pricing validation/save, new-course validation, draft/archived/empty seasons; no untranslated interface text, page errors, unexpected APIs or overflow |
| `LANGUAGE_ADMIN_ACTIONS=1 audit-english-admin.mjs` | 36 strict desktop/mobile states: next-season conflict/create, failed/completed season deletion, failed/completed class creation/copy/deletion, generated names, class save, incomplete/completed enrollment publication, failed/completed archive and status changes. English native confirmations are accepted; synthetic state assertions retain source-season and registration records. |
| `LANGUAGE_ADMIN_ACCOUNTS=1 audit-english-admin.mjs` | 96 strict desktop/mobile states: 44 duty checks cover failed/retried leave approval/rejection, substitute assignment, emergency/final confirmation, manual correction, upcoming/missing-time/late/cancelled/leave/direct-invitation states, empty results and load errors. 52 account/payment checks cover registered/unverified/legacy/admin states, registration conflicts, enabling/disabling, desktop public-profile linking, empty filters, bank/QR validation, publication conflicts/readback errors, retry/discard, account creation and enabling/disabling, and payment access denial/recovery. Authored reasons and leading zeroes remain unchanged; mobile input sizes and focused in-dialog feedback are asserted. |
| `LANGUAGE_LEGACY_ENROLLMENT=1 audit-english-interactions.mjs` | 10 desktop/mobile states on the mounted anniversary form: missing name/contact, submit error, invalid source and completion. Failed submits retain authored Chinese notes and names, phone leading zeroes and option values; success clears the form. All eight mobile controls have computed font size of at least 16px. |
| `audit-english-auth.mjs` | 34 desktop/mobile states with a zh-TW browser locale and English website setting: direct invalid-login/unconfirmed-email errors, server fallback throttling/service/required-fields/empty-response errors, network failure, password visibility, register validation/throttling/service/existing-account/weak-password errors and email-confirmation success. Names, phone leading zeroes and running results remain intact; confirmation clears only the password. Mobile controls are at least 16px. Every authentication request is intercepted; no accounts or email are created. |
| `LANGUAGE_ADMIN_MEDIA=1 audit-english-admin.mjs` | 28 strict desktop/mobile states: image access/format/8 MB and 15 MB size failures, retained crop dialog and successful retry/save; video access/format/folder/size errors; synchronization script permission/configuration/unlinked/missing-season errors and recovery. Failed media requests leave saved content unchanged. |
| `audit-english-planner.mjs` | 26 strict states: English/Traditional Chinese round trip preserves an unsaved draft without refetching; templates, failed/retried save, CSV headers and original content, copy previous week, history, next week, empty save, no selection, empty students/plans, load failure and access denial at 1440px/375px |
| `audit-english-system.mjs` | 12 strict desktop/mobile states: actual 404 route, error screen/details/reset, content loading, failure and successful retry; temporary error preview existed only in an isolated checkout and was removed before production build |
| `LANGUAGE_PUBLIC_COURSES=1 audit-english-interactions.mjs` | 60 desktop/mobile English states covering every home preview, all 24 city/level combinations, empty results, cleared filters, language switches and persisted filters; compares class membership across Traditional Chinese and English and requires the displayed count to equal visible course cards |
| `LANGUAGE_ADMIN_WORKSPACE=1 audit-english-admin.mjs` | 24 desktop/mobile states covering initial loading, sign-in requirement, permission denial, unavailable service, fallback read error, recovery and empty overview/student/coach/season/product/payment-account workspaces |
| `LANGUAGE_ACCOUNT_MENUS=1 audit-english-interactions.mjs` | 14 desktop/mobile states: student/coach/admin account navigation, notification read/mark-read failures and recovery; role-specific links remain correct and menu descriptions wrap |
| Extended finance/media audits | 36 finance states, including bank-file validation and generated column/import-limit errors with retry; 34 media/sync states, including persisted synchronization failures without rewriting stored messages |
| Unit tests | 198 tests passed, including unchanged specification values, cart identifiers and backend validation under English display; structured feedback preserves authored text and covers every selectable sleep/fatigue/completion combination. Guards check fixed Chinese literals in administrator, coach-duty, payment-settings, legacy signup, login/register, media-upload and integration-script APIs, plus generated action/size messages and actual payment validation failures. Additional guards inspect response/error fields in nine remaining service routes and eleven shared helpers, plus generated bank column/import-limit messages. Weekday grouping and sorting accept published short forms and editor-generated full forms without changing source data. |
| Static checks | TypeScript, ESLint, Traditional Chinese check, and production build passed |

Strict mode for the role scripts fails on untranslated visible interface text, option labels, attributes, page errors, unexpected API requests, or horizontal document overflow. Screenshots were also inspected because native browser control text is not represented in DOM text scans.

The earlier public release `930abe5` passed 46 local routes plus a four-route read-only check on the production domain. Those public checks do not prove the new role changes are deployed.

The student, coach, and finance checkpoint `935cbe9`, administrator/shop release `c0d54df`, and content/season/planner release `8ff4bcc` were pushed; their production deployments reached Ready with the formal domain assigned and homepage HTTP 200. The content/season/planner checkpoint used deployment `dpl_4N7HyKdVWXCrAEuqBYrXBYp7uSMG`. Their isolated production build, TypeScript, ESLint, Traditional Chinese checks and 160 unit tests passed. The existing 53-check administrator audit and 31-state registration/notification/public-coach interaction audit passed again. A CSV regression verifies translated fixed headers while retaining authored Chinese notes and leading zeroes in phone numbers and transfer codes.

Content save checks compare the synthetic payload before and after saving in English, including unrelated sections and original Chinese copy. Course saves retain Chinese weekday values and Taiwan time. Screenshots wait for finite entrance animations and capture their finished state; content panels must be visible during text inspection. Desktop content, mobile course fields and avatar cropping were visually inspected.

The planner language-switch regression was reproduced before the fix: switching to Traditional Chinese replaced `QA draft 原文保留` with the saved `Current workout`. The final check preserves the draft and makes no additional plan request during either language switch. The planner/error checkpoint passes 160 unit tests, TypeScript, ESLint, Traditional Chinese checks and an isolated production build; it was released in `8ff4bcc`. The temporary system preview server was stopped after verification.

The coach action-feedback regression was reproduced before the fix: a rejected check-in displayed its error behind the open session dialog, with no alert inside it. All 42 new states pass after the fix, and the previous 28-state coach audit passes again. Desktop feedback and the mobile leave-conflict form were visually inspected. TypeScript, ESLint, Traditional Chinese checks, 160 unit tests and an isolated production build pass. These coach changes were released with the enrollment/student checkpoint. The fixtures validate rendered responses and outgoing payloads, not real coach authorization or database transitions.

The enrollment/student checkpoint passes 48 new states plus the original 19 student, 28 coach and 31 public/notification interaction states. Its synthetic feedback uses the actual generated multiline format rather than a plain English placeholder; both reader surfaces assert English system fields without changing stored values. The mobile supplement form was visually inspected. TypeScript, ESLint, Traditional Chinese checks, 163 unit tests and an isolated production build pass. Commits `b6a2631` and `9b3cff2` were pushed to main; production deployment `dpl_Fb7w6tzCuadzBWf31QogWfFgHx73` reached Ready, the formal domain resolved to that deployment, and its homepage returned HTTP 200. This deployment check did not repeat authenticated business actions.

The administrator/anniversary checkpoint passes 36 season-action states and 10 anniversary-form states. The anniversary regression was reproduced as `請填寫Name。`; all eight mobile form controls previously computed to 14px. After the fix, the error is English and those controls are 16px. Mobile form and season-result screenshots were visually inspected. TypeScript, ESLint, Traditional Chinese checks, 167 unit tests and an isolated production build pass. These changes are local, pending release. Mocked season actions validate presentation and outgoing payloads, not real database transitions.

The administrator-account checkpoint reproduces untranslated payment-setting errors and image alternative text, plus a mobile creation error outside the active dialog. After the fix, all 96 expanded states pass. The mobile error screenshot shows readable English feedback within the form and retained bank/account values. TypeScript, ESLint, Traditional Chinese checks, 169 unit tests and an isolated production build pass. These changes remain local, pending release; simulated administrative mutations never alter real accounts, bank settings or coach assignments.

The authentication/media checkpoint reproduced Chinese login/register errors in both desktop and mobile dialogs before adding the missing translations. All 34 auth states and 28 media/integration states pass afterward. Mobile registration and crop-failure screenshots were visually inspected. TypeScript, ESLint, Traditional Chinese checks, 175 unit tests and an isolated production build pass. These changes remain local pending release. Authentication is simulated, media requests are intercepted, and synchronization code is synthetic.

The administrator/anniversary, account and authentication/media checkpoints above were subsequently pushed as `10c745e`, `882b870` and `68644bc`. Deployment `dpl_Drk6KrEZnxZ37Vhw3zkoGSKv4F3j` reached Ready; the formal domain resolved to this deployment and returned HTTP 200. This supersedes their earlier pending-release status, but is not authenticated business-flow verification.

The public-course checkpoint reproduced untranslated preview/empty-state copy and a functional English regression: the reported course count was positive while weekday groups contained no cards. Course defaults and ordering now resolve before display translation; filters use original course values, with a separate localized presentation. All 60 course states and 24 administrator workspace states pass locally. The mobile preview, empty results and access-denied screen were visually inspected. The notification trigger also renders its dynamic accessible label in the selected language immediately. The existing 31 interaction states pass again, as do 23 affected public pages (home, schedule, team and ten class/detail-registration pairs): HTTP 200, English document language, populated main content, no Chinese interface text/attributes, no navigation overlap or horizontal overflow, and the shared system/Segoe UI/Arial font stack. These new changes are local pending release. A build accidentally shared the development cache and failed; that result and the concurrent page audit were discarded, the cache was replaced, and an isolated production build passed.

The shared-service checkpoint reproduced Chinese account-menu descriptions and notification mark-read failures. It adds missing bank parser, finance access, pricing, notification, payment-detail and synchronization messages; dead validation utilities and retired endpoints are excluded. Account navigation now says “My account” for every role, and its descriptions wrap instead of truncating. All 14 account-menu states, 36 finance states and 34 media/synchronization states pass, with 198 unit tests, type/lint/Traditional Chinese checks and an isolated production build. These changes are local pending release. A read-only Safari session verified that the real administrator overview and student workspace load in English; this is limited evidence for the existing deployed version, not the new release or independent coach/student/finance roles. No real business records were edited or emails sent.

The course/shared-service changes were subsequently pushed through `02d982a`. Production deployment `dpl_8e9dkaS5YuaMQaGMRvbLdsczNEna` reached Ready with the formal domain assigned. A fresh read-only production sweep of all 46 discovered public and signed-out routes passed: HTTP 200, English document language, no untranslated visible interface text or attributes, no navigation overlaps, no horizontal overflow or page errors, and the expected system/Segoe UI/Arial font stack. The current published content snapshot has no untranslated values. The public product API currently returns an empty product list; unpublished product names and original editor values are not published English copy.

Read-only authenticated Safari checks on this release covered administrator overview, students, coaches, seasons, products, content center, payment accounts and the finance lock screen. They uncovered a legacy course name with a space after the year, which the known-format translator missed. It now accepts that whitespace, full weekday prefixes and a space before the class name, while leaving unknown custom names unchanged. The concurrently added finance-profile card also has complete English copy. Expanded local account checks pass 24 desktop/mobile states, including finance-only menu/profile access and exclusion from the three other roles. The mobile finance card was visually inspected. All 198 tests, static checks and an isolated production build pass for these follow-up fixes; deployment verification follows separately.

The follow-up fixes were pushed as `000d54c`, with deployment `dpl_FSZs6e6txaz65TxYLitLJ2FK255g` reaching Ready. Further live coach inspection found a missing space before the registration count; `bbc1cf4` now renders the whole English summary sentence. All 28 local coach states passed again, as did type/lint checks. Its production deployment `dpl_5kRatS7UMB4rbQ43BH9qDRh5QVvg` reached Ready with the formal domain assigned. A 375px production sweep passed all 46 discovered routes with HTTP 200, English document language, populated main content, the expected font stack, no untranslated visible UI/attributes, navigation overlap, overflow or page errors. The course schedule and local finance-profile mobile screenshots were inspected. The dynamic DOM regression also passed again for same-frame updates, attributes, titles, font stack and unchanged original input values.

Live authenticated coach checks included the workspace, attendance, student list and populated registration details using the existing administrator session. The attendance selector showed translated legacy class names after deployment; personal names and original registration answers remain unchanged. Finance was verified only at the real lock screen, and its unlocked/action states remain covered by intercepted local fixtures. Safari stopped returning page content during the final profile navigation, then reported that the user had changed the app; no further native browser actions were attempted. Independent signed-in student/finance checks and the last live coach-summary readback remain unverified. No production business records were changed and no emails or bank files were submitted.

Safari later recovered. The existing signed-in account profile and expanded attendance panel loaded in English. Final coach readback on the production domain confirmed “Today you have 0 assigned students and 0 group training registrations to follow up.” and translated legacy Monday/Tuesday class names; the temporary verification tab was closed. The formal domain resolved to Ready deployment `dpl_EJzDoFNYX7e3z3GiWFT1ErCueeBL` after the documentation commit. This completes the coach-summary readback above. Independent student and unlocked finance verification remain pending: automatic approval review rejected creating/deleting a disposable production student without explicit authorization, so no fixture was created. The prepared verification script passed syntax and lint checks but has not run. The user has been asked either to authorize that fixture lifecycle or sign in with an existing test student, and to unlock the real finance page.

## Still required before whole-site completion

- Administrator alternate duty/account/payment actions, full-workspace access/load failure and empty data have local coverage above. Live authenticated role verification remains to be checked; archived seasons are covered by the content/season audits.
- Shop and product-editor checks above use synthetic products and intercepted APIs; live authenticated workflows still require separate verification.
- Coach alternate states and server failures are covered locally above; live authenticated verification remains part of final release checks.
- Fixed errors from active service routes and shared finance/pricing/notification helpers have static coverage; representative error rendering and recovery have local UI coverage. The group signup page directs to LINE and does not mount the legacy form. Current native file controls are hidden behind website labels or custom crop/upload controls; the student screenshot picker is covered above.
- Source-scan entries also include unmounted components, retired routes, test fixture labels, metadata, language-conditional fragments and authored content. They do not directly count visible UI defects. Final rendered checks must continue to distinguish those categories from remaining interface text.
- Recheck the final release on the production domain and record exactly which authenticated flows used real isolated fixtures versus mocked API responses.

Names, user-authored notes, uploaded filenames, and submitted values must not be silently rewritten to eliminate Chinese from audit results. Keep authored content distinct from system interface copy.
